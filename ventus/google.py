# google.py


class Google:
    """Handles google searches
    """
    def __init__(self):
        pass

    def search(self, query: str, max_result: int) -> list:
        """Searches given query and returns a list of results

        Args:
            query (str): Query string
            max_results (int): Defines the maximum results in returned lists

        Returns:
            list: List of search results (url)
        """
        pass
